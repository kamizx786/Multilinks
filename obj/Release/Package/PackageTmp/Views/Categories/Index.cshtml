@model IEnumerable<Multilinks.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Category_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category_Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category_Picture)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Category_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category_Type)
        </td>
        <td>
            <img src="@Url.Content(item.Category_Picture)" width="50px" height="50px" />
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Category_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Category_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Category_ID })
        </td>
    </tr>
}

</table>
